# Rename this file to secrets.toml and fill in real values.
# Streamlit Community Cloud: paste this into the app's "Secrets" settings panel instead.

GROQ_API_KEY = "your-groq-api-key-here"

# Pinecone (replaces the local ChromaDB dense index)
PINECONE_API_KEY = "your-pinecone-api-key-here"
PINECONE_INDEX_NAME = "sec-10k-rag"
